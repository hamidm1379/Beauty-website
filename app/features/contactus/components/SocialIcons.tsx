// Re-export for backward compatibility. The canonical component now lives
// in app/shared so both the contact page and the footer share one source.
export { default } from "@/app/shared/components/SocialIcon";
